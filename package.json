{
  "name": "url-shortener",
  "private": true,
  "scripts": {
    "build": "npm ci && npm run build:client",
    "build:client": "cd client-app && npm ci && npm run build",
    "dev:netlify": "netlify dev"
  },
  "dependencies": {
    "@netlify/functions": "^2.8.2",
    "mysql2": "^3.14.0"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "netlify-cli": "^19.0.0",
    "typescript": "^5.7.3"
  }
}
